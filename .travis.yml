language: php
php:
- 5.4
- 5.5
- 5.6
- 7
- hhvm

install:
- composer install
script:
- vendor/bin/pake $TASK

env:
- TASK="phpunit"

matrix:
  allow_failures:
    - php: 7
    - php: hhvm

cache:
  directories:
  - vendor

branches:
  only:
    - master
